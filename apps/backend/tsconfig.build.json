{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // 检查用 tsconfig 的 rootDir 为 "."（覆盖 test/ 的 e2e 文件）；
    // 构建输入只有 src，锁回 ./src 保住 dist/main.js 布局（start:prod 依赖）
    "rootDir": "./src"
  },
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"]
}
